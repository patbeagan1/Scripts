#jax, written by patrick beagan

import io
import os

with io.open('jax.conf', 'w') as file:
    if (os.stat('jax.conf').st_size == 0):
	print ("This is the first time jax has been accessed on this account. Please provide your name.")
	name = raw_input()
	print "hello", name
